const observations = [
  { audit: 'AUD-2026-1042', branchCode: 'BR-1042', branch: 'Andheri West Branch', date: '2026-08-22', category: 'Cash Management', checkpoint: 'Cash retention beyond permissible limit', auditor: 'Amit Kulkarni', remark: 'Cash balance exceeded the prescribed limit.', informed: 'No', action: '-', voucher: 'VCH-7788', amount: '2500', transaction: '2026-08-20', risk: 'High', riskCategory: 'Cash & Vault', workflow: 'Observation Closure Pending' },
  { audit: 'AUD-2026-1077', branchCode: 'BR-1077', branch: 'Connaught Place Branch', date: '2026-08-21', category: 'KYC & Onboarding', checkpoint: 'Customer record incomplete', auditor: 'Neha Sharma', remark: 'Missing supporting identity document.', informed: 'No', action: '-', voucher: 'VCH-7741', amount: '850', transaction: '2026-08-12', risk: 'Medium', riskCategory: 'Customer Risk', workflow: 'Observation Closure Pending' },
  { audit: 'AUD-2026-1156', branchCode: 'BR-1156', branch: 'Salt Lake Sector V Branch', date: '2026-08-22', category: 'Operations', checkpoint: 'Maker-checker control not followed', auditor: 'Rahul Menon', remark: 'Manager approval was not recorded.', informed: 'No', action: '-', voucher: 'VCH-8112', amount: '12000', transaction: '2026-08-19', risk: 'High', riskCategory: 'Process Compliance', workflow: 'Rework Required' },
  { audit: 'AUD-2026-1258', branchCode: 'BR-1258', branch: 'Vastrapur Branch', date: '2026-08-20', category: 'Vault & Custody', checkpoint: 'Dual custody register incomplete', auditor: 'Rohit Shetty', remark: 'Closing register requires correction.', informed: 'No', action: '-', voucher: 'VCH-8030', amount: '5000', transaction: '2026-08-18', risk: 'Medium', riskCategory: 'Cash & Vault', workflow: 'Observation Closure Pending' },
  { audit: 'AUD-2026-1091', branchCode: 'BR-1091', branch: 'Hadapsar Branch', date: '2026-08-19', category: 'Cash Management', checkpoint: 'Cash balancing difference', auditor: 'Rohit Shetty', remark: 'Cash balancing difference remains unresolved.', informed: 'No', action: '-', voucher: 'VCH-7991', amount: '1500', transaction: '2026-08-17', risk: 'High', riskCategory: 'Cash & Vault', workflow: 'Observation Closure Pending' }
];
let filtered = [...observations];
let page = 1;
const pageSize = 3;
let attachmentQueue = [];
const columnKeys = [['audit', 'Audit No.'], ['branchCode', 'Branch Code'], ['branch', 'Branch Name'], ['date', 'Audit Date'], ['category', 'Category'], ['checkpoint', 'Checkpoint Description'], ['auditor', 'Auditor Name'], ['remark', 'Auditor Remark'], ['informed', 'Informed to Branch'], ['action', 'Action Taken by Branch']];
let columnFilters = {};
let sortState = null;

function shell(content) { document.body.innerHTML = `<div class="app-main"><header class="app-header"><div><span class="header-parent">CAP Module</span><b>/</b><strong>List Open Observations</strong></div><div class="app-user"><span class="avatar">RS</span> Rohit Shetty <small>Auditor</small></div></header><main class="app-content">${content}</main></div>`; }
function button(label, action) { return `<button class="btn ${action === 'perform' || action === 'download' ? 'primary' : 'secondary'}" data-observation-action="${action}">${label}</button>`; }
function pageMarkup() { return `<div class="page-heading"><div><span class="eyebrow">AUDITOR WORK QUEUE</span><h1>List Open Observations</h1><p>Open observations across all audits and branches assigned to you.</p></div><span class="total-chip">${filtered.length} open observations</span></div><div class="filters"><label>From Date<input id="fromDate" type="date" value="2026-04-01"></label><label>To Date<input id="toDate" type="date" value="2026-08-26"></label><button class="btn primary" data-observation-action="apply">Apply</button><button class="btn secondary" data-observation-action="reset">Reset</button><label class="search"><span>⌕</span><input id="observationSearch" placeholder="Search Audit / Branch / Category / Remark"></label></div><div class="observation-tools"><button class="btn secondary" data-observation-action="download-all">Excel Export</button><button class="btn secondary" data-observation-action="bulk-download">Bulk Download</button><label class="btn secondary file-btn">Bulk Upload<input type="file" accept=".xlsx,.csv" data-observation-action="bulk-upload"></label></div><div class="list-head"><h2>Open Observations</h2><span>${filtered.length} records</span></div><div class="table-wrap"><table class="open-observations-table"><thead><tr>${columnKeys.map(([key, label]) => `<th>${label}<span class="column-controls"><input aria-label="Search ${label}" value="${columnFilters[key] || ''}" data-column-key="${key}" placeholder="Search"><button type="button" aria-label="Sort ${label}" data-sort-key="${key}">${sortState?.key === key ? (sortState.direction === 1 ? '↑' : '↓') : '↕'}</button></span></th>`).join('')}<th>Action</th></tr></thead><tbody>${rows()}</tbody></table></div><div class="pagination"><span>Page ${page} of ${Math.max(1, Math.ceil(filtered.length / pageSize))}</span><button class="btn secondary" data-observation-action="previous" ${page === 1 ? 'disabled' : ''}>Previous</button><button class="btn secondary" data-observation-action="next" ${page >= Math.ceil(filtered.length / pageSize) ? 'disabled' : ''}>Next</button></div>`; }
function rows() { const start = (page - 1) * pageSize; const columnFiltered = filtered.filter(item => Object.entries(columnFilters).every(([key, value]) => !value || String(item[key] || '').toLowerCase().includes(value))); if (sortState) columnFiltered.sort((a, b) => String(a[sortState.key] || '').localeCompare(String(b[sortState.key] || '')) * sortState.direction); return columnFiltered.slice(start, start + pageSize).map((item, index) => `<tr data-observation-index="${observations.indexOf(item)}"><td>${item.audit}</td><td>${item.branchCode}</td><td>${item.branch}</td><td>${item.date}</td><td>${item.category}</td><td>${item.checkpoint}</td><td>${item.auditor}</td><td>${item.remark}</td><td>${item.informed}</td><td>${item.action}</td><td><button class="link-btn icon-action" aria-label="View observation" data-observation-action="view" data-index="${observations.indexOf(item)}"></button><button class="link-btn icon-action" aria-label="Perform observation" data-observation-action="perform" data-index="${observations.indexOf(item)}"></button></td></tr>`).join('') || '<tr><td colspan="11">No open observations match the current filters.</td></tr>'; }
function dialog(item, mode) { const rework = item.workflow === 'Rework Required'; const editable = mode === 'perform'; const content = `<dialog open class="observation-dialog open-observation-dialog"><button class="dialog-close" data-observation-action="close-dialog" aria-label="Close">×</button><span class="eyebrow">${editable ? 'PERFORM OPEN OBSERVATION' : 'VIEW OPEN OBSERVATION'}</span><h2>${item.audit}</h2><div class="dialog-details"><span><small>Audit Number</small><b>${item.audit}</b></span><span><small>Audit Date</small><b>${item.date}</b></span><span><small>Branch</small><b>${item.branchCode} - ${item.branch}</b></span><span><small>Auditor Name</small><b>${item.auditor}</b></span><span><small>Audit Week</small><b>17 Aug - 22 Aug 2026</b></span><span><small>Voucher</small><b>${item.voucher} / INR ${item.amount}</b></span><span><small>Transaction Date</small><b>${item.transaction}</b></span><span><small>Category</small><b>${item.category}</b></span><span><small>Checkpoint Description</small><b>${item.checkpoint}</b></span><span><small>Risk Classification</small><b>${item.risk}</b></span><span><small>Risk Category</small><b>${item.riskCategory}</b></span><span><small>Observation Creation Date</small><b>${item.date}</b></span><span><small>Auditor Remarks</small><b>${item.remark}</b></span><span><small>Auditor Attachment(s)</small><b>None</b></span><span><small>Branch Attachment(s)</small><b>None</b></span></div>${editable ? `<form id="performObservationForm" class="form-grid"><label>Informed to Branch<select name="informed" required><option value="">Select</option><option>Yes</option><option>No</option></select></label><label class="wide">Action Taken by Branch<textarea name="action" required rows="3"></textarea></label>${rework ? '<label class="wide">Updated Checkpoint Description<input name="checkpoint" value="' + item.checkpoint + '"></label><label>Updated Risk Classification<select name="risk"><option>' + item.risk + '</option><option>Medium</option><option>Low</option></select></label><label>Updated Risk Category<input name="riskCategory" value="' + item.riskCategory + '"></label><label class="wide">Updated Auditor Remarks<textarea name="remark" rows="3">' + item.remark + '</textarea></label>' : ''}<label class="wide">Supporting Attachment<input type="file" name="attachment"></label><button class="btn primary" type="submit">${rework ? 'Submit Rework' : 'Close Observation'}</button></form>` : `<p class="upload-summary">${rework ? 'Audit History: Rework Required. Manager remark and previous values are retained.' : 'Read-only observation details.'}</p>`}</dialog>`; document.body.insertAdjacentHTML('beforeend', content); if (editable) document.getElementById('performObservationForm').onsubmit = event => { event.preventDefault(); item.status = 'Closed'; item.informed = new FormData(event.target).get('informed'); item.action = new FormData(event.target).get('action'); closeDialog(); filtered = filtered.filter(row => row !== item); render(); alert(rework ? 'Rework submitted for review.' : 'Observation closed successfully.'); }; }
function closeDialog() { document.querySelectorAll('.observation-dialog').forEach(item => item.remove()); }
function exportRows(items, audit = 'All') { const header = ['Sr. No.', 'Audit Number', 'Voucher Number', 'Voucher Amount', 'Transaction Date', 'Category', 'Checkpoint Description', 'Risk Classification', 'Risk Category', 'Auditor Name', 'Auditor Remarks', 'Informed to Branch', 'Action Taken by Branch']; const data = items.map((item, index) => [index + 1, item.audit, item.voucher, item.amount, item.transaction, item.category, item.checkpoint, item.risk, item.riskCategory, item.auditor, item.remark, item.informed, item.action]); const csv = [header, ...data].map(row => row.map(value => `"${String(value).replaceAll('"', '""')}"`).join(',')).join('\n'); const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([csv], { type: 'application/vnd.ms-excel' })); link.download = `Open_Observations_${audit}_${new Date().toISOString().replace(/[-:T]/g, '').slice(0, 14)}.xlsx`; link.click(); URL.revokeObjectURL(link.href); }
function render() { shell(pageMarkup()); bind(); }
function filterRows() { const query = document.getElementById('observationSearch').value.toLowerCase().trim(); const from = new Date(document.getElementById('fromDate').value || '1900-01-01'); const to = new Date(document.getElementById('toDate').value || '2999-12-31'); filtered = observations.filter(item => new Date(item.date) >= from && new Date(item.date) <= to && (!query || Object.values(item).join(' ').toLowerCase().includes(query))); page = 1; render(); }
function bind() {
  document.getElementById('observationSearch').oninput = filterRows;
  document.querySelectorAll('[data-column-key]').forEach(input => input.oninput = () => { columnFilters[input.dataset.columnKey] = input.value.toLowerCase().trim(); page = 1; render(); });
  document.querySelectorAll('[data-sort-key]').forEach(control => control.onclick = () => { const key = control.dataset.sortKey; sortState = sortState?.key === key ? { key, direction: sortState.direction * -1 } : { key, direction: 1 }; page = 1; render(); });
  document.querySelectorAll('[data-observation-action]').forEach(control => {
    if (control.type === 'file') {
      control.onchange = () => {
        const file = control.files[0];
        if (!file) return;
        if (!/\.(xlsx|csv)$/i.test(file.name)) return alert('Upload the system-generated .xlsx template only.');
        attachmentQueue = [...filtered];
        if (!confirm('Do you want to attach supporting documents to any of the observations?')) return alert('Bulk upload processed successfully.');
        document.body.insertAdjacentHTML('beforeend', `<dialog open class="observation-dialog"><h2>Select observations for attachment</h2><p class="modal-copy">Selected observations remain open until their attachments are submitted.</p>${attachmentQueue.map((item, index) => `<label><input type="checkbox" data-attachment-index="${index}"> ${item.audit} - ${item.voucher} - ${item.category}</label>`).join('<br>')}<button class="btn primary" data-observation-action="finish-attachments">Continue</button></dialog>`);
        bind();
      };
      return;
    }
    control.onclick = () => {
      const action = control.dataset.observationAction;
      if (action === 'view' || action === 'perform') dialog(observations[Number(control.dataset.index)], action);
      if (action === 'close-dialog') closeDialog();
      if (action === 'download-all') exportRows(filtered);
      if (action === 'bulk-download') {
        const audit = prompt('Enter Audit Number to download Open Observations:');
        if (!audit) return;
        const selected = filtered.filter(item => item.audit.toLowerCase() === audit.toLowerCase());
        if (!selected.length) return alert('No Open Observations are available for the entered Audit Number.');
        exportRows(selected, audit);
      }
      if (action === 'apply') filterRows();
      if (action === 'reset') {
        document.getElementById('fromDate').value = '2026-04-01';
        document.getElementById('toDate').value = '2026-08-26';
        document.getElementById('observationSearch').value = '';
        filterRows();
      }
      if (action === 'previous') { page--; render(); }
      if (action === 'next') { page++; render(); }
      if (action === 'finish-attachments') { closeDialog(); alert('Selected observations remain open for attachment completion.'); }
    };
  });
}
render();
