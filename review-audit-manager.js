const audits = [
  { number: 'AUD-2026-1008', code: '1001', branch: 'Pune Main', week: '10 Aug - 15 Aug 2026', date: '2026-08-15', auditor: 'Rohit Shetty', manager: 'Priya Nair', approval: '18-Aug-2026 15:30', status: 'Completed' },
  { number: 'AUD-2026-1042', code: '1001', branch: 'Pune Main', week: '17 Aug - 22 Aug 2026', date: '2026-08-22', auditor: 'Rohit Shetty', manager: 'Priya Nair', approval: '-', status: 'Pending Audit Manager Review' },
  { number: 'AUD-2026-1077', code: '1002', branch: 'Mumbai Central', week: '17 Aug - 22 Aug 2026', date: '2026-08-21', auditor: 'Neha Sharma', manager: 'Priya Nair', approval: '-', status: 'Rework Required' },
  { number: 'AUD-2026-1091', code: '1003', branch: 'Nashik Branch', week: '03 Aug - 08 Aug 2026', date: '2026-08-08', auditor: 'Rahul Menon', manager: 'Priya Nair', approval: '-', status: 'WIP' },
  { number: 'AUD-2026-1156', code: '1002', branch: 'Mumbai Central', week: '10 Aug - 15 Aug 2026', date: '2026-08-15', auditor: 'Vikram Shah', manager: 'Priya Nair', approval: '-', status: 'Resubmitted for Review' }
];
const detailStages = ['Geo Tag', 'Branch Details', 'Open Observations', 'Closed Observations', 'Detailed Observations', 'Summary'];
let filtered = [...audits];
let page = 1;
const pageSize = 3;
function shell(content) { document.body.innerHTML = `<div class="app-main"><header class="app-header"><div><span class="header-parent">CAP Module</span><b>/</b><strong>Review Audit</strong></div><div class="app-user"><span class="avatar">AM</span> Priya Nair <small>Audit Manager</small></div></header><main class="app-content">${content}</main></div>`; }
function markup() { return `<div class="page-heading"><div><span class="eyebrow">AUDIT MANAGER / READ ONLY</span><h1>Review Audit</h1><p>View audit history and current status. Approval and rework decisions are handled in Perform Audit Review.</p></div><span class="total-chip">${filtered.length} audits</span></div><div class="filters"><label>From Date<input id="fromDate" type="date" value="2026-04-01"></label><label>To Date<input id="toDate" type="date" value="2026-08-26"></label><button class="btn primary" data-review-list="apply">Apply</button><button class="btn secondary" data-review-list="reset">Reset</button><label class="search"><span>⌕</span><input id="auditSearch" placeholder="Search Audit / Branch / Auditor / Status"></label></div><div class="list-head"><h2>Audit Listing</h2><span>${filtered.length} records</span></div><div class="table-wrap"><table class="review-audit-table"><thead><tr><th>Audit No.</th><th>Branch Code</th><th>Branch Name</th><th>Audit Week</th><th>Audit Date</th><th>Auditor Name</th><th>Audit Manager Name</th><th>Audit Manager Approve Date</th><th>Status</th><th>Action</th></tr></thead><tbody>${rows()}</tbody></table></div><div class="pagination"><span>Page ${page} of ${Math.max(1, Math.ceil(filtered.length / pageSize))}</span><button class="btn secondary" data-review-list="previous" ${page === 1 ? 'disabled' : ''}>Previous</button><button class="btn secondary" data-review-list="next" ${page >= Math.ceil(filtered.length / pageSize) ? 'disabled' : ''}>Next</button></div>`; }
function rows() { const start = (page - 1) * pageSize; return filtered.slice(start, start + pageSize).map((item, index) => `<tr><td>${item.number}</td><td>${item.code}</td><td>${item.branch}</td><td>${item.week}</td><td>${item.date}</td><td>${item.auditor}</td><td>${item.manager}</td><td>${item.approval}</td><td><span class="status-pill status-${item.status.toLowerCase().replaceAll(' ', '-')}" ><i></i>${item.status}</span></td><td><button class="link-btn icon-action" aria-label="View audit" data-review-list="view" data-index="${audits.indexOf(item)}">👁</button></td></tr>`).join('') || '<tr><td colspan="10">No audits match the current filters.</td></tr>'; }
function tabContent(stage, audit) { const read = (label, value) => `<div><small>${label}</small><b>${value}</b></div>`; if (stage === 0) return `<h2>Geo Tag</h2><p class="muted">Read-only location captured during audit.</p><div class="metric-grid">${read('Latitude', '19.1358 N')}${read('Longitude', '72.8261 E')}${read('Date & Time of Capture', '23-Aug-2026, 10:42 AM')}</div>`; if (stage === 1) return `<h2>Branch Details</h2><div class="metric-grid">${read('Branch Code', audit.code)}${read('Branch Name', audit.branch)}${read('Branch Address', '12 MG Road')}${read('City / State', 'Mumbai / Maharashtra')}${read('Audit Date', audit.date)}${read('Auditor Name', audit.auditor)}${read('Audit Manager Name', audit.manager)}</div>`; if (stage === 2) return `<h2>Open Observations</h2><p class="muted">No open observations are available for modification in Review Audit.</p><div class="table-wrap"><table><thead><tr><th>Audit No.</th><th>Audit Date</th><th>Category</th><th>Checkpoint Description</th><th>Auditor Remarks - Latest</th><th>Action</th></tr></thead><tbody><tr><td>${audit.number}</td><td>${audit.date}</td><td>Cash Management</td><td>Cash retention beyond permissible limit</td><td>Read-only observation record</td><td><button class="link-btn" data-review-detail="view">View</button></td></tr></tbody></table></div>`; if (stage === 3) return `<h2>Closed Observations</h2><p class="muted">Closed observations and closure details are read only.</p><div class="table-wrap"><table><thead><tr><th>Audit No.</th><th>Category</th><th>Checkpoint Description</th><th>Closure Date</th><th>Auditor Remark - Latest</th><th>Action</th></tr></thead><tbody><tr><td>${audit.number}</td><td>Cash Management</td><td>Cash count mismatch</td><td>${audit.date}</td><td>Evidence reviewed and accepted.</td><td><button class="link-btn" data-review-detail="view">View</button></td></tr></tbody></table></div>`; if (stage === 4) return `<h2>Detailed Observations</h2><p class="muted">Voucher-wise observations captured during the audit.</p><div class="table-wrap"><table><thead><tr><th>Voucher Number</th><th>Voucher Amount</th><th>Transaction Date</th><th>Category</th><th>Checkpoint Description</th><th>Risk Category</th><th>Risk Classification</th><th>Created Date</th><th>Action</th></tr></thead><tbody><tr><td>VCH-7788</td><td>INR 2,500</td><td>20-Aug-2026</td><td>Cash Management</td><td>Cash retention beyond permissible limit</td><td>Cash & Vault</td><td>Medium</td><td>${audit.date}</td><td><button class="link-btn" data-review-detail="view">View</button></td></tr></tbody></table></div>`; return `<h2>Summary</h2><p class="muted">All summary information and calculated totals are read only.</p><div class="metric-grid">${read('Cash Management', 'Completed')}${read('KYC & Onboarding', 'Completed')}${read('Vault & Custody', 'Not applicable')}${read('Cash Total', 'INR 25,000')}${read('ATM Total', 'INR 18,500')}${read('Grand Total', 'INR 43,500')}${read('Cash Denomination Remarks', 'Count verified')}${read('ATM Denomination Remarks', 'Count verified')}</div><div class="upload-summary"><strong>Audit History</strong><br>Audit created: 17-Aug-2026 09:00<br>Audit submitted: 22-Aug-2026 16:20<br>Manager review status: ${audit.status}<br>Audit Manager Approve Date: ${audit.approval}</div>`; }
function openDetail(audit) { let stage = 0; const draw = () => { closeDetail(); document.body.insertAdjacentHTML('beforeend', `<dialog open class="observation-dialog review-detail-dialog"><button class="dialog-close" data-review-detail="close" aria-label="Close">×</button><span class="eyebrow">REVIEW AUDIT / READ ONLY</span><h2>${audit.number}</h2><div class="context-bar"><b>${audit.code}</b><span>${audit.branch}</span><span>Audit Week: ${audit.week}</span><span>Status: ${audit.status}</span></div><div class="audit-tabs">${detailStages.map((name, index) => `<button class="audit-stage-tab ${index === stage ? 'active' : ''}" data-review-detail-stage="${index}">${index + 1}. ${name}</button>`).join('')}</div><div class="review-detail-body">${tabContent(stage, audit)}</div><div class="flow-footer"><button class="btn secondary" data-review-detail="previous" ${stage === 0 ? 'disabled' : ''}>Previous</button><button class="btn secondary" data-review-detail="next" ${stage === 5 ? 'disabled' : ''}>Next</button></div></dialog>`); bindDetail(draw, value => { stage = value; }); }; draw(); }
function closeDetail() { document.querySelectorAll('.review-detail-dialog').forEach(dialog => dialog.remove()); }
function filterRows() { const query = document.getElementById('auditSearch').value.toLowerCase().trim(); const from = new Date(document.getElementById('fromDate').value || '1900-01-01'); const to = new Date(document.getElementById('toDate').value || '2999-12-31'); filtered = audits.filter(item => new Date(item.date) >= from && new Date(item.date) <= to && (!query || Object.values(item).join(' ').toLowerCase().includes(query))); page = 1; render(); }
function bindDetail(draw, setStage) { const dialog = document.querySelector('.review-detail-dialog'); dialog.querySelectorAll('[data-review-detail-stage]').forEach(tab => tab.onclick = () => { setStage(Number(tab.dataset.reviewDetailStage)); draw(); }); dialog.querySelector('[data-review-detail="close"]').onclick = closeDetail; dialog.querySelector('[data-review-detail="previous"]')?.addEventListener('click', () => { setStage(Math.max(0, Number(dialog.querySelector('.audit-stage-tab.active').dataset.reviewDetailStage) - 1)); draw(); }); dialog.querySelector('[data-review-detail="next"]')?.addEventListener('click', () => { setStage(Math.min(5, Number(dialog.querySelector('.audit-stage-tab.active').dataset.reviewDetailStage) + 1)); draw(); }); dialog.querySelectorAll('[data-review-detail="view"]').forEach(button => button.onclick = () => alert('Observation details are read only.')); }
function bind() { document.getElementById('auditSearch').oninput = filterRows; document.querySelectorAll('[data-review-list]').forEach(control => control.onclick = () => { const action = control.dataset.reviewList; if (action === 'view') openDetail(audits[Number(control.dataset.index)]); if (action === 'apply') filterRows(); if (action === 'reset') { document.getElementById('fromDate').value = '2026-04-01'; document.getElementById('toDate').value = '2026-08-26'; document.getElementById('auditSearch').value = ''; filterRows(); } if (action === 'previous') { page--; render(); } if (action === 'next') { page++; render(); } }); }
function render() { shell(markup()); bind(); }
render();
